/*====================================================================
<status_led.hpp>
・通信アクティビティ表示用LEDの共通ヘルパー (xiao-esp32-s3_can2io/src/status_led.hpp 移植)

CANでホストからの指令フレームを受信するたびに短時間点灯し、
一定時間フレームが来なければ自動的に消灯する。

Copyright (c) 2026.
====================================================================*/

#pragma once

// 有効なフレームを受信した際に呼び出す。LEDを点灯し消灯タイマーをリセットする。
void statusLedInit();

// 有効なフレームを受信した際に呼び出す。LEDを点灯し消灯タイマーをリセットする。
void statusLedPulse();

// loop()から毎回呼び出す。点灯から一定時間が経過していれば消灯する。
void statusLedUpdate();
